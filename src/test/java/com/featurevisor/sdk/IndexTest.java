package com.featurevisor.sdk;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class IndexTest {

    @Test
    public void testIndexIsFunction() {
        // This test verifies that the index functionality works
        // The TypeScript test just checks that true is true, so we'll do the same
        assertTrue(true);
    }
}
